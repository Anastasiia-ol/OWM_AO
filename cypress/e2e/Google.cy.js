import * as Auth from "../utils/Helper/pageObject/Auth";
describe.skip('Social Logins Demo', () => {
    let emailData = `anastasia.oliyarnyk+ci01@scrumlaunch.com`;
    let passwordData = `21250178OwM`;
   /*beforeEach(() => {
      // can provide Facebook, Google, or Microsoft here
      cy.logIntoGoogle();
      cy.visit('https://stage.owm.ai/auth')
    })*/
  
    it('navigates to the app after navigation and displays the sample project header', () => {
        //Auth.login(emailData, passwordData);
        
      //cy.get('button:nth-child(1) div:nth-child(1) span:nth-child(3)').should('contain', 'Individual');
    })
  })